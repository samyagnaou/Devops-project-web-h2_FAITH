@using Faith.Shared.Constants

<MudNavMenu>
    <AuthorizeView Roles="@($"{Roles.Mentor},{Roles.Student}")">
        <MudNavLink Href="timeline" Match="NavLinkMatch.Prefix" Icon="@Icons.Filled.Timeline">Timeline</MudNavLink>
    </AuthorizeView>
    <AuthorizeView Roles="@Roles.Mentor">
        <MudNavLink Href="community" Match="NavLinkMatch.Prefix" Icon="@Icons.Filled.Groups">Community</MudNavLink>
    </AuthorizeView>
    <AuthorizeView Roles="@Roles.Admin">
        <MudNavLink Href="users" Match="NavLinkMatch.Prefix" Icon="@Icons.Filled.SupervisedUserCircle">Users</MudNavLink>
    </AuthorizeView>
</MudNavMenu>